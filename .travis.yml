language: go
go:
  - 1.11.x

os:
  - linux
  - linux-ppc64le
services:
  - docker

# Only build on master, stable branches and tag
# The stable branch and tag should be leading with numbers
branches:
  only:
    - master
    - /^[0-9]+\..*$/
    - /^v[0-9]+\..*$/
    - /^release-[0-9]+\..*$/

before_install:
  - source travis-env.sh

install:
  - make deps

script:
  - make lint test build image

# after_success:
#   - make push

deploy:
  skip_cleanup: true
  provider: script
  script: make docker-login release

after_deploy:
  - |
      curl -Lo travis_after_all.py https://raw.github.com/dmakhno/travis_after_all/master/travis_after_all.py
      python travis_after_all.py https://travis.ibm.com/api
      export $(cat .to_export_back)
      if [[ "$BUILD_LEADER" == "YES" ]]; then
        if [[ "$BUILD_AGGREGATE_STATUS" == "others_succeeded" ]]; then
          echo "All jobs succeeded! Creating multi arch image..."
          make manifest-tool multi-arch
        else
          echo "Some jobs failed" && exit 1
        fi
      fi
